services:
  bot:
    build: .
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - DB_PATH=${DB_PATH}
      - DEFAULT_TIMEZONE=${DEFAULT_TIMEZONE}
    volumes:
      - ./storage:/app/storage
